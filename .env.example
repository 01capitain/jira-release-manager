# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

NODE_ENV="production"

# Next Auth
# You can generate a new secret on the command line with:
# npx auth secret
# https://next-auth.js.org/configuration/options#secret
AUTH_SECRET=""

# Next Auth Discord Provider
AUTH_DISCORD_ID=""
AUTH_DISCORD_SECRET=""

# Prisma
# https://www.prisma.io/docs/reference/database-reference/connection-urls#env
DATABASE_URL="postgresql://postgres:password@localhost:5432/jira-release-manager"

# Context7
# Go to https://context7.com/dashboard to create an API key
CONTEXT7_API_KEY=""

JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_PROJECT_KEY=HK

# OpenTelemetry
# Server-side service identifier (Node SDK)
OTEL_SERVICE_NAME=jira-release-manager
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://localhost:4318/v1/traces
OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://localhost:4318/v1/metrics
# Optional shared endpoint overrides
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_DEBUG=false
# Client-side service identifier (browser SDK) – prefixed with NEXT_PUBLIC so it's bundled
NEXT_PUBLIC_OTEL_SERVICE_NAME=jira-release-manager-web
NEXT_PUBLIC_OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://localhost:4318/v1/traces
NEXT_PUBLIC_OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://localhost:4318/v1/metrics
# NEXT_PUBLIC_OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# NEXT_PUBLIC_OTEL_DEBUG=false

# Observability sandbox (Grafana) credentials
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=hotelkit123
