// "Boolean" appears in names and strings but not as a field type
model Booleanator {
  id Int @id @default(autoincrement())
  description String @default("Contains the word Boolean but is harmless")
}

enum SomeEnum {
  Boolean
  TRUE
  FALSE
}