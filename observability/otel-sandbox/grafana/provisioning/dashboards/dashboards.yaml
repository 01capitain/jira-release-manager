apiVersion: 1

providers:
  - name: OTel Sandbox
    folder: OTel Sandbox
    type: file
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
